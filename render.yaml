services:
  - type: web
    name: syw-esc-chatbot
    runtime: python
    buildCommand: pip install --upgrade pip wheel && pip install -r chatbot/requirements.txt
    startCommand: cd chatbot && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: ALLOWED_ORIGINS
        sync: false

